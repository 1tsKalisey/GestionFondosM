[pytest]
pythonpath = src
testpaths = tests
asyncio_mode = auto
addopts = 
    -ra
    --strict-markers
    --strict-config
markers =
    asyncio: async test functions
    integration: integration test
    slow: slow test
